// a c program to check voting eligibility by if else
#include<stdio.h>
int main()
{
    int age;
    printf("enter the age : ");
    scanf("%d",&age);
    if(age>=18)
    printf("person is eligibile to vote");
    else
    printf("person is not eligible to vote ");
    return 0;
}