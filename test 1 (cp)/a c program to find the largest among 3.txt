// a c program to find the largest among the 3 numbers
#include<stdio.h>
int main()
{
    int a,b,c;
    printf("enter the values: ");
    scanf("%d%d%d",&a,&b,&c);
    if(a>b&&a>c)
    {printf("%d is largest among 3 numbers ",a);}
    else if(b>a&&b>c)
    {printf("%d is largest among 3 numbers ",b);}
    else
    printf("%d is largest among 3 numbers ",c);
    return 0;
}